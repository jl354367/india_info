# India Improvement Index Frontend

## Setup

1. Copy `.env.example` to `.env` and set the API base URL if needed.
2. Install dependencies:
   ```sh
   npm install
   ```
3. Run the app:
   ```sh
   npm run dev
   ```

The app will be available at http://localhost:5173
